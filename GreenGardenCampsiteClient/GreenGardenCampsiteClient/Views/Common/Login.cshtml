
<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Form</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="~/css/Common/Login.css" asp-append-version="true" />
</head>
<body>
    <!-- Container chính -->
    <div class="container d-flex justify-content-between align-items-center vh-100">
        <div class="row w-100">
            <!-- Phần logo và mô tả bên trái -->
            <div class="col-md-5 d-flex flex-column justify-content-center align-items-start">
                <div class="logo mb-4">
                    <img src="https://wanee.vn/wp-content/uploads/2022/07/Campfire_icon_Pixabay_1345870-400x400.png" alt="Logo"style="align-items:c" >
                </div>
                <h2>Sông Quê Green Garden</h2>
                <p>Nơi giúp bạn hoà mình vào thiên nhiên, thư giãn, khám phá sự bình yên</p>
            </div>

            <!-- Phần form đăng nhập bên phải -->
            <div class="col-md-6 d-flex justify-content-end">
                <form asp-action="Login" method="post" class="p-5 shadow rounded bg-white" style="width: 100%; max-width: 500px; margin-right: 50px;">
                    <div class="mb-4">
                    <div class="mb-4">
                        <label for="email" class="form-label">Email/Số điện thoại/Tên đăng nhập</label>
                            <input type="text" class="form-control form-control-lg" name="email" placeholder="Email/SĐT/Tên đăng nhập" required>
                    </div>
                    <div class="mb-4">
                        <label for="password" class="form-label">Mật khẩu</label>
                            <input type="password" class="form-control form-control-lg"name="password" placeholder="Mật khẩu" required>
                    </div>
                    <button type="submit" class="btn btn-success btn-lg w-100">ĐĂNG NHẬP</button>
                    <div class="text-center mt-4">
                        <a href="#" class="text-decoration-none">Quên mật khẩu</a>
                        <div class="mt-3">HOẶC</div>
                        <div class="d-flex justify-content-between mt-3">
                            <button class="btn btn-primary btn-lg w-48">Facebook</button>
                            <button class="btn btn-white btn-lg w-48">Google</button>
                        </div>
                    </div>
                    <div class="text-center mt-4">
                        Bạn mới biết đến Sông Quê? <a href="#" class="text-decoration-none">Đăng ký</a>
                    </div>
                        @if (ViewData.ModelState != null && ViewData.ModelState.ErrorCount > 0)
                        {
                            <div style="color:red">
                                @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                                {
                                    <p>@error.ErrorMessage</p>
                                }
                            </div>
                        }
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
