
<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Form</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="~/css/Common/Login.css" asp-append-version="true" />
</head>
<body>
    <!-- Container chính -->
    <div class="container d-flex justify-content-between align-items-center vh-100">
        <div class="row w-100">
            <!-- Phần logo và mô tả bên trái -->
            <div class="col-md-5 d-flex flex-column justify-content-center align-items-start">
                <div class="logo mb-4">
                    <img src="https://wanee.vn/wp-content/uploads/2022/07/Campfire_icon_Pixabay_1345870-400x400.png" alt="Logo" style="align-items:c">
                </div>
                <h2>Sông Quê Green Garden</h2>
                <p>Nơi giúp bạn hoà mình vào thiên nhiên, thư giãn, khám phá sự bình yên</p>
            </div>

            <!-- Phần form đăng nhập bên phải -->
            <div class="col-md-6 d-flex justify-content-end">
                <form id="resetPasswordForm" class="p-5 shadow rounded bg-white" style="width: 100%; max-width: 500px; margin-right: 50px;" method="post" asp-action="ForgotPassword">
                    <h3 class="mb-4 text-center">Quên Mật Khẩu</h3>
                    <p class="text-muted text-center mb-4">Nhập địa chỉ email của bạn để nhận mã đặt lại mật khẩu</p>

                    <div class="mb-4">
                        <label for="email" class="form-label">Email</label>
                        <input type="email" name="email" class="form-control form-control-lg" id="email" placeholder="Nhập email của bạn" required>
                    </div>

                    <button type="submit" class="btn btn-success btn-lg w-100">Gửi mật khẩu mới</button>

                    <div id="responseMessage" class="text-center mt-4">
                        @if (ViewBag.Message != null)
                        {
                            <div class="alert alert-info mt-4">@ViewBag.Message</div>
                        }
                    </div>

                    <div class="text-center mt-4">
                        <a href="/login" class="text-decoration-none">Quay lại trang Đăng nhập</a>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>